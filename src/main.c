#include <criterion/criterion.h>
#include <limits.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>

// Test(simple, test) { cr_assert(0, "Hello"); }

int main() {
  printf("%ld\n", sizeof(uint16_t));
  return EXIT_SUCCESS;
}
